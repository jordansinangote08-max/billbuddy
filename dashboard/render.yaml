services:
  - type: web
    name: billbuddy-dashboard
    runtime: python
    rootDir: dashboard
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: APPS_SCRIPT_API_URL
        sync: false
      - key: DASHBOARD_API_KEY
        sync: false
